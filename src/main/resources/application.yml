spring:
  application:
    name: bpe-platform
  
  datasource:
    url: ${DB_URL:jdbc:mysql://192.168.125.61:3306/BPE_STAGE?useSSL=false&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:dkfdptmdps404}
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
  
  thymeleaf:
    cache: false
    prefix: classpath:/templates/
    suffix: .html
    mode: HTML
    encoding: UTF-8

server:
  port: 8082
  address: 0.0.0.0
  servlet:
    session:
      timeout: 1h  # 세션 타임아웃: 1시간

app:
  upload:
    dir: uploads/profiles/
  report:
    script:
      path: src/main/resources/monthly-report/getServerStatus.py
    template:
      path: src/main/resources/monthly-report/template/
    output:
      path: src/main/resources/monthly-report/
  elasticsearch:
    host: ${ELASTICSEARCH_HOST:http://192.168.125.64:9200}
    username: ${ELASTICSEARCH_USERNAME:elastic}
    password: ${ELASTICSEARCH_PASSWORD:elastic}
    chatbot:
      index: bpe_chatbot_faq
    data-chatbot:
      index-pattern: lucy_main_bac1_*
  llm:
    server:
      url: http://192.168.125.70:8088
      enabled: true
      timeout: 30000
      max-tokens: 300
      temperature: 0.2
  alpha-vantage:
    api-key: ${ALPHA_VANTAGE_API_KEY:YNT111LHUMZ3MAHU}  # Alpha Vantage API 키 (무료 발급: https://www.alphavantage.co/support/#api-key)
  data-supply-api:
    datasource:
      url: ${API_DB_URL:jdbc:mysql://203.251.205.85:3306/DATA_SUPPLY_API?useSSL=false&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true}
      username: ${API_DB_USERNAME:api}
      password: ${API_DB_PASSWORD:Qapi1!@#}
      driver-class-name: com.mysql.cj.jdbc.Driver

logging:
  level:
    com.bpe.platform: DEBUG
    org.springframework.security: DEBUG

